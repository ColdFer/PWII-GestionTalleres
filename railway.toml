[build]
builder = "railpack"
buildCommand = "npm run build"

[deploy]
preDeployCommand = ["php artisan migrate --force && php artisan db:seed --force && php artisan optimize:clear"]
startCommand = "php artisan serve --host=0.0.0.0 --port=$PORT"